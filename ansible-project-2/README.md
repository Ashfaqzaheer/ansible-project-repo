# Ansible EC2 MySQL Setup

This project uses Ansible to provision and configure:

- AWS EC2 instances
- MySQL with a root password (stored via Ansible Vault)
- App deployment (future scope)

## 📁 Folder Structure

